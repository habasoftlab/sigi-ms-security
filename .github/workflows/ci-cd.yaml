name: Java CI/CD Pipeline

on:
  push:
    branches:
      - main
      - develop
      - testing
      - 'feature/*'
  pull_request:
    branches:
      - main
      - develop
      - testing

jobs:
  build:
    name: Build Project
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'

      - name: Build project
        run: echo "TODO mvn clean install (pendiente de agregar lógica)"

  test:
    name: Run Tests
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'

      - name: Run tests
        run: echo "TODO mvn test (pendiente de agregar lógica)"

  deploy:
    name: Deploy (placeholder)
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/testing'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy
        run: echo "TODO ./deploy.sh (pendiente de agregar lógica)"